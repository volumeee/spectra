[package]
name = "spectra-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for building Spectra plugins in Rust"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
