[project]
name = "spectra-client"
version = "0.1.0"
description = "Python client SDK for Spectra headless browser infrastructure"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = ["requests>=2.28"]
